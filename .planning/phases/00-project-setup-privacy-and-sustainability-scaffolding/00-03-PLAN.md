---
phase: 00-project-setup-privacy-and-sustainability-scaffolding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/legal/privacy-policy.md
  - docs/legal/terms-of-service.md
  - docs/legal/retention-schedule.md
  - docs/legal/hosting-cap.md
  - docs/legal/shutdown-plan.md
  - docs/ops/dns.md
autonomous: false
requirements: [FND-04, FND-05, PRIV-01, PRIV-02, PRIV-03, PRIV-04, PRIV-05]
requirements_addressed: [FND-04, FND-05, PRIV-01, PRIV-02, PRIV-03, PRIV-04, PRIV-05]
user_setup:
  - service: domain-registrar
    why: "D-11: domain bought in Phase 0 so DNS records (SPF/DKIM/DMARC) can be filed; until purchased the docs use literal {ROOT_DOMAIN} placeholders"
    dashboard_config:
      - task: "Buy domain via Cloudflare Registrar (CD-02 default)"
        location: "https://dash.cloudflare.com/?to=/:account/registrar"
  - service: maintainer-residency
    why: "Open Question A3 (RESEARCH.md): privacy-policy controller declaration depends on whether maintainer is EU-resident; Hetzner DE host + EU resident = no international transfers; non-EU resident = GDPR Art. 27 representative may apply"
    dashboard_config:
      - task: "Confirm maintainer EU-residency to lock the privacy-policy controller declaration shape"
        location: "Direct confirmation from maintainer (no external dashboard)"

must_haves:
  truths:
    - "docs/legal/privacy-policy.md documents 3 separate granular consents (event_capture, public_leaderboards, email_digests) by name (PRIV-02)"
    - "docs/legal/privacy-policy.md names Plausible (managed cloud) and explicitly does NOT reference Google Analytics (PRIV-04)"
    - "docs/legal/privacy-policy.md declares Anthropic OAuth token is 'never collected' (architectural rule from CONTEXT.md <specifics>)"
    - "docs/legal/terms-of-service.md exists, mirrors hobby-project disclaimer, references shutdown-plan.md (PRIV-01)"
    - "docs/legal/retention-schedule.md tags every retention rule with the implementing phase (Pitfall 7 mitigation), names 90-day raw-events retention (PRIV-03)"
    - "docs/legal/hosting-cap.md cites the verbatim D-06 SQL definition (last_signin_at > NOW() - INTERVAL '90 days') and the 5,000 cap (Pitfall 8 mitigation)"
    - "docs/legal/shutdown-plan.md commits to 90-day notice + per-user data export inventory (Pitfall 9 mitigation) + 3-tier shutdown options"
    - "docs/ops/dns.md commits SPF / DKIM / DMARC records with placeholder shape so domain-buy day is config-only"
  artifacts:
    - path: "docs/legal/privacy-policy.md"
      provides: "Privacy policy clean-room template with granular consent + Plausible + GDPR-style language"
      contains: "event_capture"
    - path: "docs/legal/terms-of-service.md"
      provides: "ToS with hobby-project disclaimer + DCO inbound clause + shutdown-plan.md reference"
      contains: "Apache-2.0"
    - path: "docs/legal/retention-schedule.md"
      provides: "Retention table with implementing-phase tags"
      contains: "90 days"
    - path: "docs/legal/hosting-cap.md"
      provides: "5,000-cap declaration with verbatim D-06 SQL definition + waitlist/self-host overflow language"
      contains: "5,000"
    - path: "docs/legal/shutdown-plan.md"
      provides: "90-day notice + data-export inventory + transfer/wind-down/hard-close priority order"
      contains: "90 days"
    - path: "docs/ops/dns.md"
      provides: "SPF (include:amazonses.com) + DKIM placeholder + DMARC progression + from-address policy"
      contains: "include:amazonses.com"
  key_links:
    - from: "docs/legal/privacy-policy.md"
      to: "docs/legal/retention-schedule.md"
      via: "markdown link / cross-reference (90-day raw, indefinite aggregated)"
      pattern: "retention-schedule\\.md"
    - from: "docs/legal/terms-of-service.md"
      to: "docs/legal/shutdown-plan.md"
      via: "markdown link / cross-reference"
      pattern: "shutdown-plan\\.md"
    - from: "docs/legal/hosting-cap.md"
      to: "Phase 1 users.last_signin_at field"
      via: "verbatim SQL string in doc"
      pattern: "last_signin_at"
    - from: "docs/ops/dns.md"
      to: "Resend (per D-09)"
      via: "SES include chain + DKIM selector placeholder"
      pattern: "amazonses\\.com"
  numerics:
    - "5,000 (cap value, verbatim per D-06)"
    - "90 days (raw events retention, signed-in active definition, shutdown notice — three distinct uses, all 90)"
    - "3 granular consents (event_capture, public_leaderboards, email_digests)"
    - "3 shutdown options in priority order (Transfer, Wind-down, Hard close)"
    - "3 DMARC progression policies (none, quarantine, reject)"
    - "0 Google Analytics references in privacy-policy.md (PRIV-04 negative)"
---

<objective>
Implement Workstream C from 00-PATTERNS.md: ship the legal + privacy + ops documents that bind the project's data-handling commitments before any code can collect data. These docs are committed to a private repo per D-15 and become public at flip-day; their fidelity now determines what Phase 1+ implementations are obligated to honor.

Purpose: Per Pitfall 7, policy-vs-implementation drift kills trust. Per Pitfall 8, "5,000 active accounts" is meaningless without a single SQL definition all phases share. Per Pitfall 9, a shutdown plan without a per-user data-export inventory is a promise no one can keep. This plan eliminates all three drift modes.

Output: 6 net-new docs committed to git, all with explicit `{ROOT_DOMAIN}` / `{MAINTAINER_NAME}` / `{MAINTAINER_COUNTRY}` placeholders that get filled at domain-buy / EU-residency confirmation.
</objective>

<execution_context>
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/CLAUDE.md
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/.planning/PROJECT.md
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/.planning/REQUIREMENTS.md
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/.planning/ROADMAP.md
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/.planning/STATE.md
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/.planning/phases/00-project-setup-privacy-and-sustainability-scaffolding/00-CONTEXT.md
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/.planning/phases/00-project-setup-privacy-and-sustainability-scaffolding/00-RESEARCH.md
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/.planning/phases/00-project-setup-privacy-and-sustainability-scaffolding/00-PATTERNS.md
@C:/Users/jjohns/Claude/Projects/GSD-StatusLineGamification/.planning/phases/00-project-setup-privacy-and-sustainability-scaffolding/00-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Maintainer EU-residency confirmation (Open Question A3)</name>
  <what-built>This is a pre-task gate before authoring docs/legal/privacy-policy.md. Per RESEARCH.md Open Question 1 + Assumption A3 (line 1193), the privacy-policy controller declaration shape depends on whether the maintainer is EU-resident.</what-built>
  <how-to-verify>
    Confirm one of the following with the maintainer:
    1. **EU-resident** (e.g., Germany, Ireland, Netherlands, etc.) — privacy policy uses Hetzner DE host + EU resident = no international transfers; controller named in maintainer's EU country; no GDPR Art. 27 representative needed.
    2. **Non-EU resident** — privacy policy template needs an EU representative declaration per GDPR Art. 27, OR the public hosted instance accepts that GDPR Art. 27 may apply at scale; controller declared in maintainer's country with international-transfer language.
    3. **Defer** — author with `{MAINTAINER_COUNTRY}` literal placeholder and flag for legal review at flip-day per CD-04 (recommended path if uncertain).
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - "EU: {country}" (e.g., "EU: Germany") — Task 2 will hardcode the controller country
    - "non-EU: {country}" — Task 2 will add Art. 27 placeholder language
    - "defer" — Task 2 uses `{MAINTAINER_COUNTRY}` placeholder throughout
  </resume-signal>
  <acceptance_criteria>
    - User responds with one of: EU residency declaration, non-EU declaration, or "defer"
    - The choice is recorded in 00-03-SUMMARY.md so Phase 9 legal-review knows the starting assumption
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author docs/legal/privacy-policy.md + terms-of-service.md + retention-schedule.md</name>
  <files>docs/legal/privacy-policy.md, docs/legal/terms-of-service.md, docs/legal/retention-schedule.md</files>
  <read_first>
    - 00-RESEARCH.md lines 870-942 (Code Example 9 — privacy-policy.md verbatim template)
    - 00-RESEARCH.md lines 944-969 (Code Example 10 — granular consent record schema for context)
    - 00-RESEARCH.md lines 746-772 (Code Example 6 — retention-schedule.md verbatim template)
    - 00-RESEARCH.md lines 535-547 (Pitfall 7 — policy/implementation drift; tag rules with implementing phase)
    - 00-PATTERNS.md lines 183-210 (Pattern Assignments for privacy-policy.md, terms-of-service.md, retention-schedule.md)
    - 00-CONTEXT.md `<decisions>` D-06 (5,000 cap + 90-day active), `<specifics>` (Anthropic OAuth never collected, Plausible Cloud)
    - Task 1 result (EU-residency answer)
  </read_first>
  <action>
Create three legal docs. Use the EU-residency answer from Task 1 to drive the controller-declaration shape in privacy-policy.md.

(a) `docs/legal/privacy-policy.md` — Markdown verbatim from 00-RESEARCH.md § Code Example 9 (lines 872-940). Substitutions:

- `{DATE}` -> `2026-05-08` (today, ISO date)
- `{INSTANCE_HOSTNAME}` -> literal placeholder `{INSTANCE_HOSTNAME}` (locked at flip-day)
- `{MAINTAINER_NAME}` -> literal placeholder `{MAINTAINER_NAME}` (locked at flip-day; do NOT bake the user's real name from email signature)
- `{MAINTAINER_COUNTRY}` -> driven by Task 1:
  - If "EU: Germany" -> `Germany`
  - If "EU: {other}" -> the country
  - If "non-EU: {country}" -> the country, AND ADD a sentence under "Who we are": `If we cross GDPR Art. 27 thresholds, we will appoint an EU representative.`
  - If "defer" -> keep literal `{MAINTAINER_COUNTRY}` and add comment: `<!-- TODO: maintainer residency confirmed at flip-day per CD-04 legal review -->`
- `{ROOT_DOMAIN}` -> literal placeholder `{ROOT_DOMAIN}` (locked at domain-buy time)

CRITICAL content (verify after authoring):
- All three granular consents named: `event_capture`, `public_leaderboards`, `email_digests` (PRIV-02)
- Plausible analytics named with the phrase `plausible.io` or `Plausible Cloud` (PRIV-04 positive)
- Google Analytics explicitly absent (PRIV-04 negative — verify-phase-0.sh greps for this)
- Anthropic OAuth token row in the data table marked `**Never collected** -- stays on user's machine` (architectural rule from CONTEXT.md `<specifics>`)
- Lawful basis (GDPR Art. 6) tagged for each data category
- Retention column references "90 days raw, indefinite aggregated" for workflow events (links to retention-schedule.md by reference)

Add at the top of the file, as a comment block:
```
<!--
This privacy policy is a clean-room adaptation of Plausible's published policy + GDPR.eu template
per CD-04. Lawyer review deferred to visibility-flip / launch (see 00-RESEARCH.md Open Question 1).
Placeholders {ROOT_DOMAIN}, {INSTANCE_HOSTNAME}, {MAINTAINER_NAME}, {MAINTAINER_COUNTRY} are
filled at domain-buy / flip-day.
-->
```

(b) `docs/legal/terms-of-service.md` — There is no verbatim template in RESEARCH.md (Example 9 is privacy only). Author from CD-04 + 00-PATTERNS.md lines 196-198 + cross-references. Required structure (this is the deliverable, copy directly):

```markdown
<!--
Terms of Service. Clean-room adaptation per CD-04 (Plausible/Standard Ebooks/Codeberg style).
Placeholders {ROOT_DOMAIN}, {MAINTAINER_NAME} filled at flip-day. Legal review deferred to
visibility-flip / launch.
-->

# Terms of Service

**Effective:** 2026-05-08 - **Instance:** {INSTANCE_HOSTNAME} - **Service:** Claude Code Gamification Service (working title)

> These Terms apply to the public hosted instance. Self-hosted instances inherit this template
> but operate under the self-hoster's own terms.

## Service description

Claude Code Gamification Service is an open-source gamification layer for Claude Code users.
A web app, backend, and helper module turn workflow signals (token usage, sessions, context
efficiency, hook events) into quests, achievements, leaderboards, and cosmetic unlocks.

## Maintenance posture

This is a hobby project maintained on a best-effort basis. Issues and PRs are welcomed but not
guaranteed a response within any specific timeframe. The public hosted instance is capped at
5,000 active accounts; if that fills up, please run your own instance under the
[Apache-2.0 license](../../LICENSE). See `docs/legal/hosting-cap.md` for the cap definition.

## Account

You agree to:
- Provide accurate information at signup.
- Choose a public handle that does not impersonate another person, brand, or entity. (See the
  reserved-handle list at `packages/content/reserved-handles.json`; enforcement details land in
  Phase 1.)
- Not abuse rate limits, the API, or the moderation system.

You may delete your account at any time via Settings (path implemented in Phase 1, AUTH-07).
Account deletion removes raw events within the documented retention window
(`docs/legal/retention-schedule.md`).

## User content

When you contribute code via pull request, you contribute under the
[Developer Certificate of Origin](https://developercertificate.org/) and your contribution is
licensed under [Apache-2.0](../../LICENSE) (inbound = outbound). There is no Contributor License
Agreement.

When you post comments on profiles or otherwise contribute non-code content within the service,
you grant the service operator a non-exclusive, worldwide license to host and display that
content as part of running the service. You retain copyright. Content is moderated per
[CODE_OF_CONDUCT.md](../../CODE_OF_CONDUCT.md) and the moderation flow shipped in Phase 9.

## Public leaderboard participation

Public leaderboards are a separate granular consent (see `docs/legal/privacy-policy.md`). You
can withdraw consent for public leaderboards independently of other consents.

## Data handling

See [Privacy Policy](./privacy-policy.md) and [Retention Schedule](./retention-schedule.md).

## Service availability

The service is provided "as is" without warranty. Maintenance, downtime, and feature changes
are at the maintainer's discretion. The maintainer commits to providing 90 days notice and a
per-user data export before any public-instance shutdown -- see [Shutdown Plan](./shutdown-plan.md).

## Termination

The maintainer may suspend or terminate accounts that violate these terms or the Code of
Conduct. The user may delete their account at any time.

## Anti-cheat

Lightweight server-side validation operates on event ingestion to detect and discourage cheating.
The full anti-cheat policy is published separately (Phase 10). Affected users see a visible
flag plus a single-click appeal form; appeals auto-restore after 7 days unless explicitly
upheld.

## Changes to these terms

Material changes are emailed to all users 30 days before they take effect.

## Governing law

To be locked at flip-day per CD-04 legal review. Default while drafting:
{MAINTAINER_COUNTRY} law applies.

## Contact

hello@{ROOT_DOMAIN}
```

(c) `docs/legal/retention-schedule.md` — Markdown verbatim from 00-RESEARCH.md § Code Example 6 (lines 748-771). NO substitutions; the example is fully drafted.

CRITICAL Pitfall 7 mitigation: every retention rule in the table cites the implementing phase. The example already does this — do NOT remove or modify the "Implemented by" / "Phase" columns.
  </action>
  <verify>
    <automated>bash -c '[ -f docs/legal/privacy-policy.md ] && grep -q "event_capture" docs/legal/privacy-policy.md && grep -q "public_leaderboards" docs/legal/privacy-policy.md && grep -q "email_digests" docs/legal/privacy-policy.md && grep -iq "plausible" docs/legal/privacy-policy.md && ! grep -iq "google analytics" docs/legal/privacy-policy.md && grep -q "Never collected" docs/legal/privacy-policy.md && [ -f docs/legal/terms-of-service.md ] && grep -q "Apache-2.0" docs/legal/terms-of-service.md && grep -q "shutdown-plan.md" docs/legal/terms-of-service.md && grep -q "developercertificate.org" docs/legal/terms-of-service.md && [ -f docs/legal/retention-schedule.md ] && grep -q "90 days" docs/legal/retention-schedule.md && grep -iq "raw events" docs/legal/retention-schedule.md && grep -q "Phase 3" docs/legal/retention-schedule.md && grep -q "Phase 1" docs/legal/retention-schedule.md'</automated>
  </verify>
  <done>
    Three legal docs exist, privacy-policy.md names all 3 consents + Plausible + zero GA + Anthropic-never-collected, terms-of-service.md cites Apache-2.0 inbound + DCO + shutdown-plan ref, retention-schedule.md tags rules with implementing phase.
  </done>
  <acceptance_criteria>
    - `[ -f docs/legal/privacy-policy.md ]` returns 0
    - `grep -c "event_capture" docs/legal/privacy-policy.md` >= 1
    - `grep -c "public_leaderboards" docs/legal/privacy-policy.md` >= 1
    - `grep -c "email_digests" docs/legal/privacy-policy.md` >= 1
    - `grep -ic "plausible" docs/legal/privacy-policy.md` >= 1
    - `grep -ic "google analytics" docs/legal/privacy-policy.md` == 0 (PRIV-04 negative; required to be absent)
    - `grep -c "Never collected" docs/legal/privacy-policy.md` >= 1 (Anthropic OAuth row)
    - `grep -ic "GDPR\\|Art. 6\\|Article 6" docs/legal/privacy-policy.md` >= 1
    - `[ -f docs/legal/terms-of-service.md ]` returns 0
    - `grep -c "Apache-2.0" docs/legal/terms-of-service.md` >= 1
    - `grep -c "shutdown-plan.md\\|Shutdown Plan" docs/legal/terms-of-service.md` >= 1
    - `grep -c "developercertificate.org" docs/legal/terms-of-service.md` >= 1
    - `grep -ic "best-effort\\|hobby project" docs/legal/terms-of-service.md` >= 1
    - `grep -c "5,000" docs/legal/terms-of-service.md` >= 1
    - `[ -f docs/legal/retention-schedule.md ]` returns 0
    - `grep -c "90 days" docs/legal/retention-schedule.md` >= 1
    - `grep -ic "raw events" docs/legal/retention-schedule.md` >= 1
    - `grep -c "Phase 1" docs/legal/retention-schedule.md` >= 1
    - `grep -c "Phase 3" docs/legal/retention-schedule.md` >= 1
    - `grep -c "AUTH-07\\|account deletion\\|Account deletion" docs/legal/retention-schedule.md` >= 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author docs/legal/hosting-cap.md + shutdown-plan.md + docs/ops/dns.md</name>
  <files>docs/legal/hosting-cap.md, docs/legal/shutdown-plan.md, docs/ops/dns.md</files>
  <read_first>
    - 00-RESEARCH.md lines 774-807 (Code Example 7 — hosting-cap.md verbatim template)
    - 00-RESEARCH.md lines 809-868 (Code Example 8 — shutdown-plan.md verbatim template)
    - 00-RESEARCH.md lines 666-744 (Code Example 5 — dns.md verbatim template)
    - 00-RESEARCH.md lines 548-566 (Pitfall 8 — hosting-cap drift; SQL definition is load-bearing; Pitfall 9 — shutdown export inventory must be concrete)
    - 00-RESEARCH.md lines 522-533 (Pitfall 6 — SPF 10-DNS-lookup limit)
    - 00-PATTERNS.md lines 209-239 (Pattern Assignments for hosting-cap.md, shutdown-plan.md, dns.md)
    - 00-CONTEXT.md `<decisions>` D-06 (5,000 + 90-day SQL definition), D-07 (waitlist + self-host link), D-09/D-11 (Resend + domain), D-12 (noreply + hello from-address policy)
  </read_first>
  <action>
Create three documents.

(a) `docs/legal/hosting-cap.md` — Markdown verbatim from 00-RESEARCH.md § Code Example 7 (lines 776-807). NO substitutions. The example is fully drafted including the verbatim D-06 SQL string and waitlist overflow language.

CRITICAL Pitfall 8 mitigation: the SQL string `SELECT COUNT(*) FROM users WHERE last_signin_at > NOW() - INTERVAL '90 days';` MUST appear verbatim. Phase 1's session schema will reference `last_signin_at` literally. Three different "active" definitions across phases is the failure mode (RESEARCH.md line 554).

(b) `docs/legal/shutdown-plan.md` — Markdown verbatim from 00-RESEARCH.md § Code Example 8 (lines 811-866). NO substitutions. The example is fully drafted including the per-user data-export inventory.

CRITICAL Pitfall 9 mitigation: the "Data export per user" subsection lists exactly which fields are exported (Profile, achievements, cosmetics, level + lifetime XP, last 90 days raw events, all quest history, all comments authored, friend graph rows). Phase 1's account-deletion path will reference these. The export endpoint itself is a post-Phase-0 backlog item — note this in the SUMMARY (Plan 04 will update ROADMAP if needed).

(c) `docs/ops/dns.md` — Markdown verbatim from 00-RESEARCH.md § Code Example 5 (lines 668-742). Substitutions:
- `{ROOT_DOMAIN}` -> literal placeholder `{ROOT_DOMAIN}` (locked at domain-buy)
- `{REGION}` (in MX placeholder) -> literal placeholder `{REGION}` (locked at Resend dashboard config; typically `us-east-1` or `eu-west-1`)
- `{SELECTOR}` and `{PUBLIC_KEY}` (DKIM) -> kept as literal placeholders (Resend issues these at domain-add)

The example already documents:
- SPF: `v=spf1 include:amazonses.com ~all` on `send.{ROOT_DOMAIN}`
- DKIM: 1-3 TXT records issued by Resend at domain-add time
- DMARC: 3-step progression `none -> quarantine -> reject` over 30+ days
- From-address policy: `noreply@` (transactional) + `hello@` (replyable) per D-12
- Pitfall 6 reference: SPF 10-lookup limit warning if a second mail provider is later added

NO custom additions beyond the verbatim template. The placeholders are intentional — DNS records committed before domain purchase per D-11 so flipping live is a config change, not a research project.
  </action>
  <verify>
    <automated>bash -c '[ -f docs/legal/hosting-cap.md ] && grep -Eq "5,?000" docs/legal/hosting-cap.md && grep -q "90 days" docs/legal/hosting-cap.md && grep -q "last_signin_at" docs/legal/hosting-cap.md && grep -q "INTERVAL" docs/legal/hosting-cap.md && grep -iq "self-host\\|docker compose up" docs/legal/hosting-cap.md && [ -f docs/legal/shutdown-plan.md ] && grep -Eq "90 days|90-day" docs/legal/shutdown-plan.md && grep -iq "data export" docs/legal/shutdown-plan.md && grep -iq "transfer\\|wind-down\\|hard close" docs/legal/shutdown-plan.md && [ -f docs/ops/dns.md ] && grep -q "include:amazonses.com" docs/ops/dns.md && grep -q "_dmarc" docs/ops/dns.md && grep -q "DKIM" docs/ops/dns.md && grep -q "noreply@" docs/ops/dns.md && grep -q "hello@" docs/ops/dns.md'</automated>
  </verify>
  <done>
    Three docs exist with all load-bearing strings verbatim from RESEARCH.md examples. hosting-cap.md cites the verbatim D-06 SQL definition; shutdown-plan.md commits to the data-export inventory and 3-tier shutdown options; dns.md commits SPF/DKIM/DMARC placeholders + from-address policy.
  </done>
  <acceptance_criteria>
    - `[ -f docs/legal/hosting-cap.md ]` returns 0
    - `grep -Ec "5,?000" docs/legal/hosting-cap.md` >= 1 (FND-04 / D-06)
    - `grep -c "90 days" docs/legal/hosting-cap.md` >= 1
    - `grep -c "last_signin_at" docs/legal/hosting-cap.md` >= 1 (Pitfall 8: load-bearing SQL field)
    - `grep -c "INTERVAL" docs/legal/hosting-cap.md` >= 1 (verbatim SQL string)
    - `grep -ic "self-host\\|docker compose up" docs/legal/hosting-cap.md` >= 1 (D-07: overflow link)
    - `[ -f docs/legal/shutdown-plan.md ]` returns 0
    - `grep -Ec "90 days|90-day" docs/legal/shutdown-plan.md` >= 1
    - `grep -ic "data export" docs/legal/shutdown-plan.md` >= 1
    - `grep -c "Transfer" docs/legal/shutdown-plan.md` >= 1
    - `grep -ic "wind-down" docs/legal/shutdown-plan.md` >= 1
    - `grep -ic "Self-hosters" docs/legal/shutdown-plan.md` >= 1
    - `grep -c "Profile" docs/legal/shutdown-plan.md` >= 1 (data-export inventory)
    - `grep -c "achievements\\|cosmetics" docs/legal/shutdown-plan.md` >= 1 (data-export inventory)
    - `[ -f docs/ops/dns.md ]` returns 0
    - `grep -c "include:amazonses.com" docs/ops/dns.md` >= 1
    - `grep -c "_dmarc" docs/ops/dns.md` >= 1
    - `grep -c "DKIM" docs/ops/dns.md` >= 1
    - `grep -c "noreply@" docs/ops/dns.md` >= 1
    - `grep -c "hello@" docs/ops/dns.md` >= 1
    - `grep -ic "p=none\\|p=quarantine\\|p=reject" docs/ops/dns.md` >= 1 (DMARC progression)
    - `grep -c "10" docs/ops/dns.md` >= 1 OR `grep -ic "lookup\\|SPF" docs/ops/dns.md` >= 1 (Pitfall 6 reference)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="non-blocking">
  <name>Task 4: Maintainer purchases domain via Cloudflare Registrar (CD-02 / D-11)</name>
  <what-built>D-11 commits to buying the domain in Phase 0 so SPF/DKIM/DMARC records can be filed. The docs are written with `{ROOT_DOMAIN}` placeholders so this is a string-substitution at flip-day, not a research project — but the actual purchase is a maintainer action. Cloudflare Registrar (CD-02 default) provides at-cost pricing + free WHOIS privacy.</what-built>
  <how-to-verify>
    1. Once a name candidate clears trademark/availability checks (see D-14: final naming deferred to Phase 9, so use a working/placeholder domain or hold this task until naming locks), navigate to https://dash.cloudflare.com -> Domain Registration -> Register Domains.
    2. Search for and purchase the domain at Cloudflare Registrar.
    3. Enable WHOIS Privacy (free with Cloudflare) and 2FA on the registrar account.
    4. Update Cloudflare DNS to point at the placeholder records from `docs/ops/dns.md` (these are the SPF/DKIM/DMARC shapes; actual DKIM values come from Resend at domain-add time -- see Plan 03 task notes).
    5. After purchase, replace `{ROOT_DOMAIN}` literals across `docs/legal/privacy-policy.md`, `docs/legal/terms-of-service.md`, `docs/legal/hosting-cap.md`, `docs/legal/shutdown-plan.md`, `docs/ops/dns.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, and `README.md` (Plan 04) with the actual domain. Single-PR substitution.
  </how-to-verify>
  <resume-signal>
    Reply with:
    - "purchased: {actual_domain}" — Claude can do the placeholder substitution PR
    - "deferred to flip-day" — placeholders stay; flag in 00-03-SUMMARY.md as carryover
    - "blocked: {reason}" — domain naming still pending per D-14 (Phase 9), continue Phase 0 with placeholders
  </resume-signal>
  <acceptance_criteria>
    - User responds with one of: domain purchased + actual name, deferred, or blocked-with-reason
    - If purchased: substitution PR is opened in a follow-up; the per-file `{ROOT_DOMAIN}` placeholder count goes to 0
    - If deferred/blocked: 00-03-SUMMARY.md records the carryover so Phase 9 (naming finalization) inherits the action
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Privacy policy → user data flow | Doc declares what data is collected, on what basis, for how long; mismatch with code = T-policy-drift |
| Hosting-cap doc → users.last_signin_at field (Phase 1) | Doc names the SQL field; Phase 1 must implement that exact field name |
| Shutdown plan → per-user export endpoint (post-Phase-0) | Doc commits to specific data fields exportable; later phase must implement them or the promise fails |
| DNS records → domain registrar | SPF/DKIM/DMARC committed before live; flip is config change |
| Maintainer email → CoC enforcement contact | Domain placeholders wait for D-11 purchase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-12 | Repudiation | Privacy-policy/actual-behavior drift (T-policy-drift) | mitigate | Retention schedule tags every rule with implementing phase (Pitfall 7); plan-check at each later phase verifies code matches policy; granular consent model (event_capture/public_leaderboards/email_digests) gives users withdrawal mechanism |
| T-00-13 | Repudiation | Hosting cap drift across phases (Pitfall 8) | mitigate | hosting-cap.md cites verbatim SQL `SELECT COUNT(*) FROM users WHERE last_signin_at > NOW() - INTERVAL '90 days'`; Phase 1 schema implements `last_signin_at` literally; verify-phase-0.sh greps `last_signin_at` |
| T-00-14 | Denial of service | Shutdown without data export (T-shutdown-no-export) | mitigate | shutdown-plan.md commits to per-user data-export inventory listing exact fields; flagged in ROADMAP as post-Phase-0 backlog so a real phase owns the export endpoint; 90-day notice + 3-tier shutdown options bound the surprise |
| T-00-15 | Spoofing | Email domain spoofing without SPF/DKIM/DMARC (T-email-spoof) | mitigate | docs/ops/dns.md commits SPF (`v=spf1 include:amazonses.com ~all`), DKIM placeholder (Resend issues at domain-add), DMARC progression (none -> quarantine -> reject); Pitfall 6 documents SPF 10-DNS-lookup limit so future second-provider doesn't break deliverability |
| T-00-16 | Information Disclosure | Anthropic OAuth token reaching backend (T-oauth-leak) | mitigate (policy) | privacy-policy.md declares Anthropic OAuth token "Never collected -- stays on user's machine"; the architectural rule ships in Plan 04 (docs/architecture/00-overview.md) and code-level enforcement is Phase 1 (Pino redaction transport) and Phase 5 (helper never sends token) |
| T-00-17 | Information Disclosure | International data transfer breach (non-EU resident maintainer) | mitigate / accept | Task 1 confirms maintainer residency; if EU, no international transfers (Hetzner DE host); if non-EU, Art. 27 representative language is added; legal review at flip-day per CD-04 catches any gap |
</threat_model>

<verification>
After all tasks: every doc exists with the verbatim grep-targets that scripts/verify-phase-0.sh asserts (PRIV-01, PRIV-02, PRIV-03, PRIV-04, FND-04, FND-05); placeholders {ROOT_DOMAIN}, {INSTANCE_HOSTNAME}, {MAINTAINER_NAME} are documented as flip-day substitutions; Task 1 (EU-residency) and Task 4 (domain purchase) outcomes recorded in 00-03-SUMMARY.md.
</verification>

<success_criteria>
- All 6 docs exist, committed to git
- privacy-policy.md names 3 granular consents + Plausible + zero Google Analytics + Anthropic-never-collected
- terms-of-service.md cites Apache-2.0 inbound + DCO + shutdown-plan ref + 5,000 cap
- retention-schedule.md tags every rule with implementing phase (Pitfall 7)
- hosting-cap.md contains the verbatim D-06 SQL string (Pitfall 8)
- shutdown-plan.md commits to 90-day notice + per-user export inventory + 3-tier shutdown options (Pitfall 9)
- dns.md commits SPF + DKIM placeholder + DMARC progression + from-address policy (D-12)
- Task 1 (EU-residency) answered or explicitly deferred
- Task 4 (domain purchase) actioned, deferred, or blocked-with-reason
- scripts/verify-phase-0.sh from Plan 02 passes all PRIV-* and FND-04/05 sections after this plan completes
</success_criteria>

<output>
After completion, create `.planning/phases/00-project-setup-privacy-and-sustainability-scaffolding/00-03-SUMMARY.md` summarizing: which legal docs are clean-room vs verbatim, the EU-residency answer from Task 1, domain purchase outcome from Task 4, list of placeholder substitutions still pending at flip-day, and any items flagged for Phase 9 legal review.
</output>
